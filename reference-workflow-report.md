# تحليل workflow في v1.sm3ha.io عبر بحث «فناير»

## الخلاصة التنفيذية

تم تتبع الرحلة الفعلية من الصفحة الرئيسية إلى البحث عن «فناير»، ثم صفحة النتائج، ثم نتيجة واحدة، ثم صفحة `media` وأخيراً `videos_dl`. الموقع يعتمد على **صفحات SEO ديناميكية من نوع `/s/{slug}`**، ويستعمل YouTube كمصدر للعنوان والصورة والمدة ومعرّف الفيديو. بعد ذلك يضع رابطاً وسيطاً opaque من نوع `/media?d=...`، ثم يمرر التحويل إلى صفحة `/videos_dl?v={youtubeVideoId}` التي تضمّن iframe من خدمة خارجية لمعالجة خيارات MP3/MP4.

## التسلسل الذي تمت ملاحظته

| المرحلة | الرابط أو السلوك | ما ظهر فعلياً |
|---|---|---|
| الصفحة الرئيسية | `https://v1.sm3ha.io/` | حقل بحث، روابط عربية جاهزة إلى `/s/{slug}`، وقائمة كبيرة من العبارات الحديثة |
| تنفيذ البحث | البحث عن «فناير» | تم توجيه المستخدم إلى `https://v1.sm3ha.io/s/فناير` |
| صفحة SEO | `/s/فناير` | عنوان «تحميل فناير Mp3 Mp4 سمعها»، وحوالي عشر نتائج مرتبطة بالكلمة |
| نتيجة YouTube | أول نتيجة | YouTube ID هو `1vQphg4luE0`، صورة من `i.ytimg.com`، ومدة 13:32 تقريباً |
| رابط المشاهدة | `#1vQphg4luE0` | Fragment محلي مبني على YouTube video ID، وليس رابط YouTube مباشر في HTML |
| رابط التحميل | `/media?d=8131528d21f12f86589c4a04b8b1cf26` | طبقة وسيطة تكشف كود الملف والمدة والجودة المعلنة |
| صفحة التحويل | `/videos_dl?v=1vQphg4luE0` | نفس كود الفيديو والمدة، مع خيارات MP3/MP4 وConvert داخل iframe خارجي |

## صفحة النتائج

الرابط النهائي للبحث هو `/s/فناير`، وهو رابط ثابت وقابل للفهرسة وليس مجرد نتيجة query مؤقتة. العنوان يتضمن الكلمة وعبارات تجارية مثل Mp3 وMp4. النتائج مبنية من metadata مرتبطة بفيديوهات YouTube، إذ ظهرت صور من نطاق `i.ytimg.com`، عناوين الفيديو، ومددها.

النتيجة الأولى كانت بعنوان «فناير تشعل الغضب بـمروكية هشام الملولي يطلب النجدة وفاجـ ــعة تقلب عرس آسفي لحزن كبير»، ومرتبطة بالمعرّف `1vQphg4luE0`. كل نتيجة تقريباً لها زوج من الروابط: رابط تحميل opaque من `/media?d=...`، ورابط مشاهدة fragment مثل `#1vQphg4luE0`.

## صفحة media

صفحة media لا تبدأ بتنزيل مباشر. هي صفحة وسيطة بعنوان «تحميل الملف» وتعرض:

| الحقل | القيمة التي ظهرت للنتيجة المختارة |
|---|---|
| كود الملف | `1vQphg4luE0` |
| المدة | 13:31 |
| الجودة المعلنة | Mp3@128kbps وMp4@720p/360p |
| زر التحويل | `DOWNLOAD NOW` إلى `/videos_dl?v=1vQphg4luE0` |
| رابط خارجي | نطاق `ub.diarizesagiest.com` بمسار opaque |

الصفحة تضع كذلك نصاً ترويجياً وإخلاء مسؤولية ينسب المسؤولية القانونية للطرف الثالث. هذا يوضح أن `/media` يعمل كـ **طبقة routing وتهيئة** قبل الوصول إلى صفحة التحويل، وليس كصفحة ملف ثابت ظاهر المصدر.

## صفحة videos_dl

بعد الضغط على `DOWNLOAD NOW` انتقل الرابط إلى `/videos_dl?v=1vQphg4luE0`. صفحة HTML الرئيسية تحتوي على تبويبي MP3 وMP4 وأزرار Convert، لكن التحليل البنيوي أكد أن هذه الواجهة تأتي من iframe اسمه `widgetApi` ومصدره:

`https://api.ytzyar.cc/load?v=1vQphg4luE0&cf=...`

الـiframe الخارجي يعلن في HTML الخاص به `noindex, nofollow, noarchive`. كما تظهر في الصفحة الرئيسية وصلة مباشرة أخرى إلى نطاق خارجي. لم يتم الضغط على Convert أو الرابط الخارجي، لذلك لا يمكن تأكيد endpoint النهائي أو شكل الملف الناتج دون تنفيذ تحويل فعلي.

## ماذا يتأرشف وماذا لا يتأرشف؟

المسار الأكثر قابلية للفهرسة هو `/s/{slug}`، لأنه يحتوي على عنوان SEO، نص، صور، ومدد وروابط داخلية كثيرة. أما `/media` و`/videos_dl` فهما مساران تشغيليان وسيطان، ويحتوي آخرهما على iframe خارجي مقيّد بـ`noindex/nofollow/noarchive`. لذلك الاستراتيجية الظاهرة هي أن Google يكتشف صفحات الكلمات `/s/...`، وليس صفحات التحويل أو الروابط الخارجية.

## الاستنتاج التقني المفيد لـNaghmaHub

لتقليد الجزء السليم من workflow، يمكن استعمال YouTube Data API لجلب metadata فقط، ثم حفظ `query` و`slug` و`provider_video_id` و`title` و`thumbnail` و`duration` في Supabase. route عام مثل `/s/:slug` يعرض صفحة عربية حقيقية لكل كلمة محفوظة، مع deduplication وروابط داخلية. بعد ذلك يمكن استعمال `/media?d=<opaque-token>` كطبقة تحقق، ثم ربط المشاهدة مباشرة بصفحة YouTube أو embed مرخّص.

لا ينبغي نسخ الرابط الخارجي أو تضمين خدمة تحويل غير موثوقة كما هي. إذا أضيف تحويل في NaghmaHub، يجب أن يكون مبنياً على محتوى يملكه الموقع أو يملك ترخيصه، مع إظهار المصدر، حقوق الاستخدام، وحالة المحتوى بوضوح. أما فيديوهات YouTube العامة فالأفضل أن تبقى metadata وروابط مشاهدة رسمية دون تنزيل أو استضافة.

## المصادر

[1]: https://v1.sm3ha.io/ "الصفحة الرئيسية لموقع سمعها"
[2]: https://v1.sm3ha.io/s/%D9%81%D9%86%D8%A7%D9%8A%D8%B1 "نتائج البحث عن فناير"
[3]: https://v1.sm3ha.io/media?d=8131528d21f12f86589c4a04b8b1cf26 "صفحة media للنتيجة المختارة"
[4]: https://v1.sm3ha.io/videos_dl?v=1vQphg4luE0 "صفحة videos_dl للنتيجة المختارة"
