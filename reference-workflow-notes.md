# ملاحظات تحليل workflow لموقع v1.sm3ha.io

## الصفحة الرئيسية — 2026-08-24

الموقع يفتح بعنوان «سمعها - تحميل وإستماع أغاني». الواجهة تحتوي على شعار، روابط «الرئيسية» و«جديد البحث»، وحقل بحث واحد بplaceholder «إبحث عن اغنية او البوم او فنان». أسفل البحث تظهر قائمة طويلة من عبارات/عناوين حديثة، وكل عنصر منها رابط إلى مسار من الشكل `/s/{slug}`. يوجد في الأسفل روابط «اتصل بنا» و«DMCA» و«Sm3ha».

الملاحظة المؤكدة من HTML: الصفحة الرئيسية تعرض روابط نتائج جاهزة مسبقاً، وتستخدم مسارات SEO عربية/مُرمّزة، ولا يظهر في HTML الأولي أي API endpoint صريح أو كود تنزيل يمكن إثباته من الصفحة وحدها.

المصدر: https://v1.sm3ha.io/

## صفحة الأغنية — `/s/{slug}`

فتح رابط SEO يقود إلى صفحة بعنوان من نمط «تحميل ... Mp3 Mp4 سمعها». الصفحة لا تعرض أغنية واحدة فقط؛ بل تعرض مجموعة نتائج مرتبطة بعبارة البحث، وكل نتيجة تتضمن صورة YouTube مصغرة، عنواناً، مدة، رابط «مشاهدة» كرابط fragment إلى YouTube ID، ورابط «تحميل» بصيغة `/media?d={32-char hex token}`.

المؤكد من HTML أن صور النتائج تأتي من `i.ytimg.com` لبعض العناصر، وأن رابط التحميل لا يكشف مباشرة رابط YouTube أو ملفاً نهائياً، بل يستخدم قيمة opaque قصيرة/سداسية عشرية. لا يمكن إثبات طريقة توليد أو تخزين هذه القيمة من HTML وحده.

المصدر: https://v1.sm3ha.io/s/%D8%A7%D8%BA%D9%86%D9%8A%D9%87-%D9%82%D9%88%D9%84%D9%88%D8%A7-%D9%84%D8%A7%D9%86%D9%86%D9%8A-%D9%85%D8%A7%D8%B2%D9%84%D8%AA

## صفحة `media?d=...`

رابط «تحميل» لا يبدأ بتنزيل مباشر؛ يفتح صفحة «تحميل الملف». الصفحة تعرض كود الملف المرتبط بمعرّف YouTube، المدة، والجودات المعلنة: MP3@128kbps وMP4@720p/360p. تظهر زران منفصلان: `DOWNLOAD NOW` يقود إلى `/videos_dl?v={youtube_id}`، و«تحميل مباشر» يقود إلى نطاق خارجي مختلف (`ub.diarizesagiest.com/...`). الصفحة تتضمن كذلك مساحة ترويجية/إعلانية ونصاً عن Google Play وإخلاء مسؤولية ينسب المسؤولية للطرف الثالث.

المؤكد: هناك طبقة وسيطة بين رابط `/media?d=...` وبين مسار `/videos_dl?v=...`، كما يوجد رابط خارجي مباشر. لا يمكن الجزم من HTML وحده هل `/videos_dl` يحوّل إلى ملف، ينشئه عند الطلب، أو يمرر إلى خدمة طرف ثالث.

المصدر: https://v1.sm3ha.io/media?d=dd87cfde50e6c936c4885a9070a94d4f

## صفحة `/videos_dl?v={youtube_id}`

المسار الثاني يعرض نفس كود الفيديو والمدة والجودة المعلنة، ثم بطاقة تحويل فيها صورة/عنوان الفيديو وتبويبان MP3 وMP4. أسفلها جدول خيارات جودة وحجم ملف، وتظهر عدة أزرار `Convert`. في الصفحة يوجد أيضاً رابط «تحميل مباشر» خارجي.

المؤكد من العناصر المرئية: التحويل يتم اختياره من خلال format tab ثم quality row ثم زر Convert. لم يتم الضغط على Convert، لذلك لا يمكن إثبات endpoint الطلب أو مدة العملية أو شكل الرابط النهائي. وجود رابط خارجي وإعلانات/نص ترويجي منفصل عن زر Convert يجب اعتباره طرفاً ثالثاً وغير موثوق حتى يتم فحصه قانونياً وتقنياً.

المصدر: https://v1.sm3ha.io/videos_dl?v=RGzs7T75_0o

## البحث

نموذج البحث يستعمل `GET /search?q=...`. عند البحث بعبارة «قولوا لها»، رد HTTP هو `302` مع `Location: /s/قولوا-لها`؛ أي أن الموقع يطبع query إلى slug عربي ثم ينقل المستخدم إلى صفحة نتائج/تفاصيل SEO بدلاً من إبقاء query في الرابط.

هذه ملاحظة سلوكية مؤكدة من HTTP headers، وليست استنتاجاً عن قاعدة البيانات أو مصدر الفهرسة.

## بحث «فناير» — 2026-08-25

رابط البحث الناتج هو `https://v1.sm3ha.io/s/فناير`، والعنوان الظاهر هو «تحميل فناير Mp3 Mp4 سمعها». الصفحة تعرض نتائج مستخرجة من YouTube، بصور من `i.ytimg.com`، عناوين ومدد الفيديو.

النتيجة الأولى تحمل YouTube video ID هو `1vQphg4luE0`، ومدتها 13:32، وعنوانها «فناير تشعل الغضب بـمروكية هشام الملولي يطلب النجدة وفاجـ ــعة تقلب عرس آسفي لحزن كبير». رابط التحميل الأول هو `https://v1.sm3ha.io/media?d=8131528d21f12f86589c4a04b8b1cf26`، بينما رابط المشاهدة هو fragment محلي `#1vQphg4luE0` وليس رابط YouTube مباشراً.

الصفحة تعرض حوالي عشر نتائج، ولكل نتيجة رابط `media?d=<opaque token>` ورابط مشاهدة مبني على YouTube video ID. هذا يؤكد أن البحث عن كلمة جديدة ينشئ أو يسترجع صفحة SEO من `/s/<slug>`، ثم يربط كل نتيجة بطبقة media وسيطة.

## تتبع نتيجة فناير عبر media وvideos_dl

بعد اختيار أول نتيجة، فتحت طبقة الوسيط على الرابط `https://v1.sm3ha.io/media?d=8131528d21f12f86589c4a04b8b1cf26`.

صفحة media تعرض «تحميل الملف»، وتكشف داخلياً كود الملف `1vQphg4luE0`، مدة 13:31، وجودات معلنة `Mp3@128kbps - Mp4@720p/360p`. فيها زر `DOWNLOAD NOW` يقود إلى `/videos_dl?v=1vQphg4luE0`، ورابط آخر «تحميل مباشر» إلى نطاق خارجي `ub.diarizesagiest.com` بمسار opaque. الصفحة تحتوي أيضاً على نص إعلاني/ترويجي وإخلاء مسؤولية يقول إن المحتوى لم ينشئه أو يستضيفه الموقع.

بعد الضغط على `DOWNLOAD NOW`، انتقل المتصفح إلى `https://v1.sm3ha.io/videos_dl?v=1vQphg4luE0`. الصفحة بقيت بعنوان «تحميل الملف»، وعرضت كود الفيديو والمدة والجودة نفسها، مع رابط «تحميل مباشر» خارجي فقط في الحالة التي ظهرت. لم يظهر زر Convert أو ملف يتم تنزيله تلقائياً، ولم أضغط على الرابط الخارجي.

## بنية صفحة التحويل والـiframe

تحليل HTML لصفحة `videos_dl` أكد وجود iframe بالمعرّف `widgetApi` ومصدره `https://api.ytzyar.cc/load?v=1vQphg4luE0&cf=...`. هذا يعني أن واجهة MP3/MP4 والجداول وأزرار Convert ليست مكونات أصلية ظاهرة في HTML الرئيسي، بل تأتي من خدمة خارجية مضمّنة.

صفحة HTML الرئيسية تحتوي على تبويبي MP3 وMP4 وأربعة أزرار Convert كعناصر داخل iframe، إضافة إلى رابط خارجي مباشر. مصدر iframe نفسه يتضمن `meta robots="noindex, nofollow, noarchive"` وفق HTML الذي تم تحميله، ما يميّز طبقة التحويل الخارجية عن صفحات `/s/...` القابلة للفهرسة.

لم يتم الضغط على Convert أو الرابط الخارجي، لذلك لم يتم تنفيذ تحويل أو تنزيل ولم يتم ادعاء endpoint نهائي غير مؤكد.
